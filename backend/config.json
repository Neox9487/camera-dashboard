{
  "server": {
    "host": "localhost",
    "port": 1000,
    "api_prefix": "/api",
    "logging": {
      "level": "INFO",
      "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    }
  },
  "database": {
    "host": "localhost",
    "port": 3306,
    "user": "user",
    "password": "password",
    "database": "dbname",
    "table": "tablename"
  }
}